name: Deploy

on:
  workflow_call:
    inputs:
      ENVIRONMENT:
        description: 'Environment name'
        type: string
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Test output
        run: echo Deploy job

  output:
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      - name: Output
        run: echo ${{ needs.deploy.job_name }}
