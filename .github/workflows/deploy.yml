name: Build

#  echo 'post-update-cmd'
#
#  ssh -p 2200 %server_name% "cd %folder_path%;
#  git pull &&
#  composer install &&
#  php artisan migrate --force &&
#  php artisan clear-compiled &&
#  php artisan cache:clear &&
#  php artisan queue:restart"

on:
  workflow_dispatch:
    inputs:
      env:
        description: 'Set environment (alpha, dev, staging)'
        required: true
        default: 'alpha'
      ec2ip:
        required: true
        default: 3.10.173.82
        description: 'host'

env:
  FOLDER_PATH: /home/c8prod/captiv8.io
  ALPHA: c8prod@platform.alpha.captiv8.io
  DEVELOP: c8prod@platform.dev.captiv8.io
  STAGING: c8prod@platform.staging.captiv8.io

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: pwd
      - run: ls -la
      - name: scp pipelines
        uses: cross-the-world/ssh-scp-ssh-pipelines@latest
        with:
          host: 3.10.173.82
          user: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          first_ssh: |
            sudo mkdir -p /home/c8prod/captiv8.io
            sudo rm -rf /home/c8prod/captiv8.io
          scp: |
            '.*' => /home/c8prod/captiv8.io
          last_ssh: |
            ls -la
            pwd
#      - id: ssh
#        uses: invi5H/ssh-action@v1
#        with:
#          SSH_HOST: 3.10.173.82
#          SSH_PORT: 22
#          SSH_USER: ubuntu
#          SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
#      - run: ssh ${{ steps.ssh.outputs.SERVER }} pwd
#      - run: ssh ${{ steps.ssh.outputs.SERVER }} touch test
