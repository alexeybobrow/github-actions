name: Deploy

on:
  workflow_dispatch:
    inputs:
      ENVIRONMENT:
        description: 'Environment name'
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Test output
        run: |
          echo Current job is ${{ github.job }}
          printenv
          # echo "JOB_NAME=${{ github.job }}" >> $GITHUB_OUTPUT

  output:
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      - name: Output
        run: echo This job is ${{ needs.deploy.env.GITHUB_JOB }}
